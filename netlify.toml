[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  port = 3000
  targetPort = 5173
  autoLaunch = false
  # Direct proxy mode - bypass header processing
  framework = "#custom"

# Headers are for production only - dev uses Vite's own handling
[[headers]]
  for = "/*"
  [headers.values]
    # XSS Protection
    X-XSS-Protection = "1; mode=block"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self'; connect-src 'self' https: ws: wss:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"

    # Clickjacking Protection
    X-Frame-Options = "DENY"

    # Content Type Protection
    X-Content-Type-Options = "nosniff"

    # Referrer Policy
    Referrer-Policy = "strict-origin-when-cross-origin"

    # Feature/Permissions Policy
    Permissions-Policy = "camera=(), microphone=(self), geolocation=(), payment=()"

    # HTTPS Enforcement
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"

    # Additional Security Headers
    X-Permitted-Cross-Domain-Policies = "none"
    X-Content-Security-Policy = "default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-inline'"

# SPA Fallback for production only
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = { "NOT_Path" = ["/api/*", "/.netlify/functions/*"] }
